# 2026年8月5日 工作日志

## 项目：二手图书借阅管理系统

---

### 一、项目审查与修复

#### 1. 数据库配置修复
- **问题**：`application.yml` 数据库地址为 WSL IP `172.28.48.1`，Windows 无法连接
- **修复**：改为 `127.0.0.1`
- **问题**：密码默认值为空，实际 MySQL 密码为 `978712`
- **修复**：设置正确默认值 `${DB_PASSWORD:978712}`

#### 2. 安全漏洞修复
- **问题**：`SysUserController` 用户分页接口返回密码 hash
- **修复**：分页和查询接口增加 `user.setPassword(null)`
- **问题**：`/user/add` 接口未加密密码、未设默认值
- **修复**：增加 BCrypt 加密、userType 和 status 默认值

#### 3. 权限配置修复
- **问题**：`SaTokenConfigure` 缺失 `/borrow/return/**`、`/borrow/delete/**` 管理员鉴权
- **修复**：补充角色校验规则
- **增加**：全局 CORS 配置

#### 4. 代码质量修复
- **问题**：`BookManageApplication.java` 含冗余阶乘计算代码
- **修复**：删除无关代码
- **问题**：`SysUser.java` 缺少 `@TableName` 注解
- **修复**：添加 `@TableName("sys_user")`
- **问题**：`LogAspect` X-Forwarded-For 可被伪造
- **修复**：取第一个有效 IP 并校验格式

#### 5. 功能完善
- **问题**：`SysLogController` 缺少按日志类型筛选
- **修复**：`/log/page` 增加 `logType` 参数
- **问题**：学生端能看到下架图书
- **修复**：`Library.vue` 默认筛选 `bookStatus: 0`
- **问题**：`Dashboard.vue` 重复请求借阅数据
- **修复**：合并为一次 `Promise.all`
- **问题**：`Book.vue` 编辑图书时 availableNum 不联动
- **修复**：totalNum 变化时自动调整 availableNum

---

### 二、前端修复

#### 1. 登录状态同步问题
- **问题**：管理员登录后显示为学生
- **修复**：`Login.vue` 添加 `await nextTick()`
- **修复**：`Layout.vue` 增加 `userType` computed 属性
- **修复**：`user.js` 增加 `loadUser` 错误处理、`refreshUser` 方法

#### 2. 借阅管理页面问题
- **问题**：点击"通过"无反应
- **修复**：所有操作函数添加 `try-catch` 错误处理
- **修复**：`nameOf` 函数改用 `Map` + `computed` 实现响应式查找
- **修复**：操作列宽度 180→200，避免按钮换行
- **修复**：借出中状态增加"删除"按钮

#### 3. Vue 模板解析错误
- **问题**：`{{ nameOf(userMap, row.userId) }}` 导致编译错误
- **修复**：拆分为 `userName(id)` 和 `bookName(id)` 独立函数

#### 4. 中文输入法兼容
- **问题**：`@input` 正则过滤干扰中文 IME
- **修复**：增加 `@compositionstart`/`@compositionend` 事件处理

#### 5. 分页参数统一
- **修复**：后端默认 `pageSize` 从 5 改为 10（与前端一致）

---

### 三、环境搭建

- 安装 Maven 3.9.16 到 `C:\Users\13992\maven`
- 安装 Node.js LTS 24.19.0
- 配置环境变量

---

### 四、项目启动验证

- 后端启动：`http://localhost:8080` ✅
- 前端启动：`http://localhost:5173` ✅
- 登录测试：admin/stu01/stu02 均正常 ✅
- 借阅流程：申请→审核→归还 验证通过 ✅

---

### 五、待完善事项

- 前端分类列表硬编码，建议增加分类接口
- 借阅下拉最多100条，建议改为服务端搜索
- 并发借阅校验无数据库锁，高并发可能穿透
- 建议增加全局异常日志记录
